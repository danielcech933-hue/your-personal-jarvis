import { useEffect, useRef, useState } from "react";
import { loadJarvisProfile, type JarvisAppearance } from "@/lib/jarvis-profile";
import { decideCompanionLife, getCompanionZonePosition, type CompanionAction, type CompanionMood } from "@/lib/companion-life";
export type AvatarState = "idle" | "listening" | "thinking" | "speaking";
export type AvatarMood = CompanionMood;
declare global { interface Window { THREE?: any; THREE_GLTFLoader?: any; THREE_VRM?: any; } }
type Props = { state: AvatarState; mood: AvatarMood; level: number; appearance?: JarvisAppearance; onAsk?: (question: string) => void; onActivity?: (activity: string) => void };
type Action = CompanionAction;
type Runtime = { vrm: any; accentLight: any; modelBaseScale: any };
const MODEL_URLS = ["https://cdn.jsdelivr.net/gh/madjin/vrm-samples@master/vroid/beta/Vita.vrm", "https://cdn.jsdelivr.net/gh/madjin/vrm-samples@master/vroid/fem_vroid.vrm"];
const DEFAULT_APPEARANCE: JarvisAppearance = { style: "glamorous", hair: "silver", outfit: "midnight", accent: "cyan", height: 1, body: "athletic" };
const clamp = (value: number, min: number, max: number) => Math.max(min, Math.min(max, value));
const labels: Record<Action, string> = { idle: "Jen tak odpočívá", walk: "Prochází se po pokoji", sit: "Sedí a relaxuje", work: "Pracuje u svého stolu", sleep: "Spí na gauči", stretch: "Protahuje se", dance: "Tancuje", spin: "Točí se pro zábavu", jump: "Skáče", flip: "Dělá salto", wave: "Mává na tebe", think: "Přemýšlí" };
const palette = { hair: { silver: 0xddebf7, black: 0x151a28, violet: 0x9f78df, rose: 0xe18aa8 }, outfit: { midnight: 0x142d4c, white: 0xdce5ef, crimson: 0x72192e }, accent: { cyan: 0x46e9ff, violet: 0xb28cff, rose: 0xff82bd } };
function bone(vrm: any, name: string) { return vrm?.humanoid?.getNormalizedBoneNode?.(name) ?? null; }
function capture(vrm: any) { const names = ["hips", "spine", "chest", "head", "leftUpperArm", "rightUpperArm", "leftLowerArm", "rightLowerArm", "leftUpperLeg", "rightUpperLeg", "leftLowerLeg", "rightLowerLeg"]; const pose: Record<string, any> = {}; for (const name of names) { const bodyBone = bone(vrm, name); if (bodyBone) pose[name] = { bone: bodyBone, q: bodyBone.quaternion.clone() }; } return pose; }
function rotate(pose: Record<string, any>, name: string, x = 0, y = 0, z = 0) { const entry = pose[name]; const T = window.THREE; if (!entry || !T?.Quaternion || !T?.Euler) return; entry.bone.quaternion.copy(entry.q).multiply(new T.Quaternion().setFromEuler(new T.Euler(x, y, z))); }
function reset(pose: Record<string, any>) { for (const entry of Object.values(pose) as any[]) entry.bone.quaternion.copy(entry.q); }
function expression(vrm: any, name: string, value: number) { try { vrm?.expressionManager?.setValue?.(name, clamp(value, 0, 1)); } catch {} }
function applyAppearance(runtime: Runtime, appearance: JarvisAppearance) { const { vrm, accentLight, modelBaseScale } = runtime; if (!vrm?.scene) return; const hairColor = palette.hair[appearance.hair]; const outfitColor = palette.outfit[appearance.outfit]; const accentColor = palette.accent[appearance.accent]; const bodyScale = appearance.body === "slim" ? 0.94 : appearance.body === "curvy" ? 1.08 : 1; const height = clamp(appearance.height, 0.9, 1.1); vrm.scene.scale.set(modelBaseScale.x * bodyScale * height, modelBaseScale.y * height, modelBaseScale.z * bodyScale * height); accentLight?.color?.setHex?.(accentColor); vrm.scene.traverse((object: any) => { const raw = object.material; const materials = Array.isArray(raw) ? raw : raw ? [raw] : []; const objectName = `${String(object?.name || "")} ${String(object?.material?.name || "")}`.toLowerCase(); for (const material of materials) { const name = `${objectName} ${String(material.name || "")}`.toLowerCase(); if (name.includes("hair")) { material.color?.setHex?.(hairColor); if ("roughness" in material) material.roughness = 0.38; if ("metalness" in material) material.metalness = appearance.style === "cyber" ? 0.12 : 0.02; } if (/dress|cloth|skirt|top|outfit|onepiece|shoe|boot|heel/.test(name)) { material.color?.setHex?.(outfitColor); if ("roughness" in material) material.roughness = appearance.style === "glamorous" ? 0.28 : 0.46; } if (/eye|iris|glow|gem|tech/.test(name)) { material.emissive?.setHex?.(accentColor); if ("emissiveIntensity" in material) material.emissiveIntensity = appearance.style === "cyber" ? 0.2 : 0.06; } if (/skin|body|face/.test(name) && !/mask|clothing|shoe/.test(name)) { if ("roughness" in material) material.roughness = 0.5; if ("metalness" in material) material.metalness = 0; } } }); }
function animate(vrm: any, pose: Record<string, any>, action: Action, t: number, level: number, mood: AvatarMood) { reset(pose); const swing = Math.sin(t * 7); const slow = Math.sin(t * 1.25); const breathe = Math.sin(t * 1.55) * 0.018; if (action === "walk") { rotate(pose, "leftUpperLeg", swing * 0.58); rotate(pose, "rightUpperLeg", -swing * 0.58); rotate(pose, "leftLowerLeg", Math.max(0, -swing) * 0.52); rotate(pose, "rightLowerLeg", Math.max(0, swing) * 0.52); rotate(pose, "leftUpperArm", -swing * 0.38); rotate(pose, "rightUpperArm", swing * 0.38); } else if (action === "wave") { rotate(pose, "rightUpperArm", -1.03, 0, -0.45); rotate(pose, "rightLowerArm", -0.2, 0, -0.9 + Math.sin(t * 10) * 0.22); } else if (action === "sit" || action === "work") { rotate(pose, "leftUpperLeg", -1.12); rotate(pose, "rightUpperLeg", -1.12); rotate(pose, "leftLowerLeg", 1.18); rotate(pose, "rightLowerLeg", 1.18); rotate(pose, "spine", action === "work" ? -0.22 : -0.14); if (action === "work") { rotate(pose, "leftUpperArm", -0.58, 0, 0.16); rotate(pose, "rightUpperArm", -0.58, 0, -0.16); rotate(pose, "leftLowerArm", -0.42); rotate(pose, "rightLowerArm", -0.42); rotate(pose, "head", 0.08, slow * 0.07, 0); } } else if (action === "sleep") { rotate(pose, "leftUpperLeg", -0.86); rotate(pose, "rightUpperLeg", -0.84); rotate(pose, "leftLowerLeg", 1); rotate(pose, "rightLowerLeg", 1.02); rotate(pose, "spine", -0.12); rotate(pose, "head", 0.23, 0, 0.18); } else if (action === "stretch") { const k = Math.sin(t * 2.2); rotate(pose, "leftUpperArm", -0.92 + k * 0.08, 0, 0.34); rotate(pose, "rightUpperArm", -0.92 + k * 0.08, 0, -0.34); rotate(pose, "leftLowerArm", -0.32, 0, 0.22); rotate(pose, "rightLowerArm", -0.32, 0, -0.22); } else if (action === "dance") { const k = Math.sin(t * 5.4); rotate(pose, "leftUpperLeg", k * 0.24); rotate(pose, "rightUpperLeg", -k * 0.24); rotate(pose, "leftUpperArm", -0.68 + k * 0.28, 0, 0.22); rotate(pose, "rightUpperArm", -0.68 - k * 0.28, 0, -0.22); rotate(pose, "chest", 0, 0, k * 0.09); rotate(pose, "head", 0, k * 0.1, 0); } else if (action === "jump") { const k = Math.sin(clamp(t / 1.15, 0, 1) * Math.PI); rotate(pose, "leftUpperLeg", -0.28 * k); rotate(pose, "rightUpperLeg", 0.28 * k); rotate(pose, "leftLowerLeg", 0.68 * k); rotate(pose, "rightLowerLeg", 0.68 * k); rotate(pose, "leftUpperArm", 0.55 * k, 0, 0.12 * k); rotate(pose, "rightUpperArm", 0.55 * k, 0, -0.12 * k); } else if (action === "flip") { rotate(pose, "leftUpperArm", 0.68); rotate(pose, "rightUpperArm", 0.68); rotate(pose, "leftUpperLeg", 0.26); rotate(pose, "rightUpperLeg", -0.26); } else if (action === "think") { rotate(pose, "rightUpperArm", -0.52, 0, -0.18); rotate(pose, "rightLowerArm", 0.28, 0, -0.42); rotate(pose, "head", 0.04, 0.1 + slow * 0.04, -0.04); } else { rotate(pose, "head", Math.sin(t * 0.85) * 0.04, Math.sin(t * 0.55) * 0.025, Math.sin(t * 0.72) * 0.025); rotate(pose, "chest", 0, 0, breathe); } const sleeping = mood === "sleep" || action === "sleep"; expression(vrm, "blink", sleeping ? 1 : Math.sin(t * 0.72) > 0.985 ? 1 : 0); expression(vrm, "aa", level > 0.03 ? Math.min(1, 0.12 + level * 0.9) : 0); expression(vrm, "happy", mood === "play" || action === "dance" ? 0.72 : mood === "curious" ? 0.22 : 0); expression(vrm, "relaxed", sleeping ? 0.58 : mood === "bored" ? 0.14 : 0); }
function visibleFallback(action: Action, a: JarvisAppearance) { return <div className="jarvis-avatar-fallback" data-hair={a.hair} data-outfit={a.outfit} data-accent={a.accent} data-body={a.body} data-style={a.style} aria-label="Živá anime postava"><div className="fallback-aura"/><div className="fallback-shadow"/><div className="fallback-character" data-action={action}><div className="fallback-hair"/><div className="fallback-head"><span className="fallback-eye left"/><span className="fallback-eye right"/><span className="fallback-mouth"/></div><div className="fallback-neck"/><div className="fallback-torso"><span className="fallback-core"/></div><div className="fallback-arm left"/><div className="fallback-arm right"/><div className="fallback-waist"/><div className="fallback-skirt"/><div className="fallback-leg left"/><div className="fallback-leg right"/></div></div>; }
function addRoom(T: any, scene: any, appearance: JarvisAppearance) { const group = new T.Group(); const accentColor = palette.accent[appearance.accent]; const floorMaterial = new T.MeshStandardMaterial({ color: 0x07111b, roughness: 0.78, metalness: 0.05 }); const furnitureMaterial = new T.MeshStandardMaterial({ color: 0x0d1b2e, roughness: 0.42, metalness: 0.16 }); const trimMaterial = new T.MeshStandardMaterial({ color: accentColor, emissive: accentColor, emissiveIntensity: 0.12, roughness: 0.35, metalness: 0.35 }); const glassMaterial = new T.MeshStandardMaterial({ color: 0x12324d, emissive: 0x082035, emissiveIntensity: 0.32, transparent: true, opacity: 0.72, roughness: 0.2, metalness: 0.1 }); const floor = new T.Mesh(new T.PlaneGeometry(7.8, 5.2), floorMaterial); floor.rotation.x = -Math.PI / 2; floor.receiveShadow = true; group.add(floor); const deskTop = new T.Mesh(new T.BoxGeometry(2.2, 0.16, 0.72), furnitureMaterial); deskTop.position.set(-1.72, 1.0, -0.38); deskTop.castShadow = true; group.add(deskTop); for (const x of [-2.62, -0.82]) { const leg = new T.Mesh(new T.BoxGeometry(0.12, 1.0, 0.12), furnitureMaterial); leg.position.set(x, 0.5, -0.38); group.add(leg); } const monitor = new T.Mesh(new T.BoxGeometry(0.9, 0.55, 0.08), trimMaterial); monitor.position.set(-1.72, 1.38, -0.43); group.add(monitor); const monitorGlow = new T.Mesh(new T.PlaneGeometry(0.72, 0.38), glassMaterial); monitorGlow.position.set(-1.72, 1.38, -0.385); group.add(monitorGlow); const sofaSeat = new T.Mesh(new T.BoxGeometry(1.75, 0.48, 0.82), furnitureMaterial); sofaSeat.position.set(1.0, 0.34, 0.28); sofaSeat.castShadow = true; group.add(sofaSeat); const sofaBack = new T.Mesh(new T.BoxGeometry(1.75, 1.0, 0.22), furnitureMaterial); sofaBack.position.set(1.0, 0.72, -0.03); group.add(sofaBack); for (const x of [0.25, 1.75]) { const arm = new T.Mesh(new T.BoxGeometry(0.2, 0.65, 0.82), furnitureMaterial); arm.position.set(x, 0.58, 0.28); group.add(arm); } const windowGlass = new T.Mesh(new T.PlaneGeometry(2.5, 1.65), glassMaterial); windowGlass.position.set(0.45, 2.15, -1.25); group.add(windowGlass); for (const x of [-0.8, 1.7]) { const post = new T.Mesh(new T.BoxGeometry(0.06, 1.8, 0.08), trimMaterial); post.position.set(x, 2.15, -1.2); group.add(post); } for (const y of [1.28, 3.02]) { const bar = new T.Mesh(new T.BoxGeometry(2.56, 0.06, 0.08), trimMaterial); bar.position.set(0.45, y, -1.2); group.add(bar); } scene.add(group); return group; }
export function JarvisCompanion({ state, mood, level, appearance = DEFAULT_APPEARANCE, onActivity }: Props) { const [liveAppearance, setLiveAppearance] = useState<JarvisAppearance>(() => loadJarvisProfile().appearance || appearance); const [vrmReady, setVrmReady] = useState(false); const [fallbackAction, setFallbackAction] = useState<Action>("idle"); const mountRef = useRef<HTMLDivElement | null>(null); const propsRef = useRef({ state, mood, level }); const activityRef = useRef(onActivity); const appearanceRef = useRef(liveAppearance); const runtimeRef = useRef<Runtime | null>(null); useEffect(() => { propsRef.current = { state, mood, level }; }, [state, mood, level]); useEffect(() => { activityRef.current = onActivity; }, [onActivity]); useEffect(() => { const next = appearance || DEFAULT_APPEARANCE; appearanceRef.current = next; setLiveAppearance(next); if (runtimeRef.current) applyAppearance(runtimeRef.current, next); }, [appearance]); useEffect(() => { const handler = () => { const next = loadJarvisProfile().appearance || DEFAULT_APPEARANCE; appearanceRef.current = next; setLiveAppearance(next); if (runtimeRef.current) applyAppearance(runtimeRef.current, next); }; window.addEventListener("jarvis-profile-updated", handler); return () => window.removeEventListener("jarvis-profile-updated", handler); }, []); useEffect(() => { const mount = mountRef.current; if (!mount) return; let cancelled = false; let timer = 0; let raf = 0; const setAction = (action: Action) => { setFallbackAction(action); activityRef.current?.(labels[action]); }; const fallbackOnly = () => { setVrmReady(false); let until = 0; const choose = (now: number) => { const p = propsRef.current; const decision = decideCompanionLife(new Date(), p.mood); const action: Action = p.state !== "idle" ? p.state === "thinking" ? "think" : "wave" : decision.action; until = now + (action === "sleep" ? 7.5 : 2 + Math.random() * 3); setAction(action); }; const frame = (now: number) => { if (cancelled) return; if (!until || now >= until) choose(now); raf = requestAnimationFrame(frame); }; raf = requestAnimationFrame(frame); return () => cancelAnimationFrame(raf); }; const loadModel = async (loader: any) => { let lastError: unknown; for (const url of MODEL_URLS) { try { return await new Promise<any>((resolve, reject) => loader.load(url, resolve, undefined, reject)); } catch (error) { lastError = error; } } throw lastError || new Error("No VRM model could be loaded"); }; const boot = async () => { if (cancelled) return; if (window.THREE?.__jarvisLoadError || window.THREE_GLTFLoader?.__jarvisLoadError || window.THREE_VRM?.__jarvisLoadError) { timer = window.setTimeout(() => void fallbackOnly(), 0); return; } if (!window.THREE || !window.THREE_GLTFLoader || !window.THREE_VRM) { timer = window.setTimeout(boot, 100); return; } const T = window.THREE; try { const { GLTFLoader } = window.THREE_GLTFLoader; const { VRMLoaderPlugin, VRMUtils } = window.THREE_VRM; const scene = new T.Scene(); const width = Math.max(mount.clientWidth, 1); const height = Math.max(mount.clientHeight, 1); const camera = new T.PerspectiveCamera(23, width / height, 0.01, 100); camera.position.set(0, 1.62, 5.65); camera.lookAt(0, 1.27, -0.1); const renderer = new T.WebGLRenderer({ alpha: true, antialias: true, powerPreference: "high-performance" }); renderer.setPixelRatio(Math.min(window.devicePixelRatio, 1.75)); renderer.setSize(width, height, false); renderer.outputColorSpace = T.SRGBColorSpace; renderer.toneMapping = T.ACESFilmicToneMapping; renderer.toneMappingExposure = 1.18; renderer.shadowMap.enabled = true; renderer.shadowMap.type = T.PCFSoftShadowMap; renderer.domElement.className = "jarvis-3d-canvas"; mount.appendChild(renderer.domElement); scene.add(new T.HemisphereLight(0xd9f5ff, 0x06101b, 2.4)); const key = new T.DirectionalLight(0xffffff, 3.8); key.position.set(-3.5, 6.5, 5.5); key.castShadow = true; key.shadow.mapSize.set(1024, 1024); scene.add(key); const accentLight = new T.PointLight(palette.accent[appearanceRef.current.accent], 13, 12); accentLight.position.set(-3.2, 2.7, 2.2); scene.add(accentLight); const warm = new T.PointLight(0xffb66a, 2.4, 9); warm.position.set(3.2, 1.6, 2.4); scene.add(warm); addRoom(T, scene, appearanceRef.current); const loader = new GLTFLoader(); loader.register((parser: any) => new VRMLoaderPlugin(parser)); const gltf = await loadModel(loader); if (cancelled) return; const vrm = gltf.userData.vrm; if (!vrm) throw new Error("VRM model missing"); VRMUtils?.removeUnnecessaryVertices?.(vrm.scene); VRMUtils?.combineSkeletons?.(vrm.scene); vrm.scene.traverse((object: any) => { object.castShadow = true; object.receiveShadow = true; }); const bounds = new T.Box3().setFromObject(vrm.scene); const size = bounds.getSize(new T.Vector3()); const center = bounds.getCenter(new T.Vector3()); const uniformScale = 2.55 / Math.max(size.y, 0.001); vrm.scene.scale.setScalar(uniformScale); vrm.scene.position.set(-center.x * uniformScale, -bounds.min.y * uniformScale, -center.z * uniformScale); scene.add(vrm.scene); const runtime: Runtime = { vrm, accentLight, modelBaseScale: new T.Vector3(uniformScale, uniformScale, uniformScale) }; runtimeRef.current = runtime; applyAppearance(runtime, appearanceRef.current); const pose = capture(vrm); let x = 0; let z = getCompanionZonePosition("center").z; let tx = x; let tz = z; let action: Action = "idle"; let started = performance.now() / 1000; let until = 0; let yaw = 0; let mouseX = 0; let mouseY = 0; let lastAppearanceKey = ""; const choose = (now: number) => { const p = propsRef.current; if (p.state !== "idle") { action = p.state === "thinking" ? "think" : p.state === "listening" ? "wave" : "idle"; started = now; until = now + 1.8; tx = x; tz = z; setAction(action); return; } const decision = decideCompanionLife(new Date(), p.mood); const target = getCompanionZonePosition(decision.zone); action = decision.action; started = now; until = now + decision.durationMs / 1000; tx = target.x; tz = target.z; setAction(action); }; const onPointer = (event: PointerEvent) => { mouseX = event.clientX / Math.max(innerWidth, 1) * 2 - 1; mouseY = event.clientY / Math.max(innerHeight, 1) * 2 - 1; }; const resize = () => { const nextWidth = Math.max(mount.clientWidth, 1); const nextHeight = Math.max(mount.clientHeight, 1); camera.aspect = nextWidth / nextHeight; camera.updateProjectionMatrix(); renderer.setSize(nextWidth, nextHeight, false); }; addEventListener("pointermove", onPointer, { passive: true }); addEventListener("resize", resize); const resizeObserver = new ResizeObserver(resize); resizeObserver.observe(mount); choose(performance.now() / 1000); setVrmReady(true); let previous = performance.now(); const frame = (ms: number) => { if (cancelled) return; const now = ms / 1000; const dt = Math.min((ms - previous) / 1000, 0.05); previous = ms; const currentAppearance = appearanceRef.current; const appearanceKey = JSON.stringify(currentAppearance); if (appearanceKey !== lastAppearanceKey) { lastAppearanceKey = appearanceKey; applyAppearance(runtime, currentAppearance); } if (now >= until) choose(now); const travelAction = action === "walk" || action === "work" || action === "sit" || action === "sleep" || action === "think"; if (travelAction) { const dx = tx - x; const dz = tz - z; const distance = Math.hypot(dx, dz); if (distance > 0.03) { const speed = action === "work" ? 0.68 : action === "sit" || action === "sleep" ? 0.82 : 1.0; const step = Math.min(speed * dt, distance); x += dx / distance * step; z += dz / distance * step; yaw = Math.atan2(dx, dz); } } x = clamp(x, -2.55, 2.55); z = clamp(z, -1.0, 0.95); vrm.scene.position.x = x; vrm.scene.position.z = z; const phase = clamp((now - started) / Math.max(until - started, 0.01), 0, 1); if (action === "jump") vrm.scene.position.y = Math.sin(clamp((now - started) / 1.15, 0, 1) * Math.PI) * 0.45; else if (action === "flip") { vrm.scene.position.y = Math.sin(phase * Math.PI) * 0.32; vrm.scene.rotation.x = phase * Math.PI * 2; } else { vrm.scene.position.y = 0; vrm.scene.rotation.x = 0; } if (action === "spin") vrm.scene.rotation.y = yaw + phase * Math.PI * 2; else { const targetYaw = yaw + (action === "wave" ? Math.PI * 0.06 : 0); vrm.scene.rotation.y += clamp(targetYaw - vrm.scene.rotation.y, -0.09, 0.09); } animate(vrm, pose, action, now - started, propsRef.current.level, propsRef.current.mood); if (pose.head?.bone) { pose.head.bone.rotation.y = clamp(mouseX * 0.2, -0.2, 0.2); pose.head.bone.rotation.x += clamp(-mouseY * 0.07, -0.07, 0.07); } if (propsRef.current.state === "speaking") expression(vrm, "aa", Math.min(1, 0.18 + propsRef.current.level * 0.9)); vrm.update?.(dt); renderer.render(scene, camera); raf = requestAnimationFrame(frame); }; raf = requestAnimationFrame(frame); return () => { cancelAnimationFrame(raf); resizeObserver.disconnect(); removeEventListener("pointermove", onPointer); removeEventListener("resize", resize); runtimeRef.current = null; renderer.dispose(); renderer.domElement.remove(); scene.traverse((object: any) => { object.geometry?.dispose?.(); const materials = object.material ? (Array.isArray(object.material) ? object.material : [object.material]) : []; materials.forEach((material: any) => material.dispose?.()); }); }; } catch (error) { console.error("JARVIS 3D model failed", error); runtimeRef.current = null; setVrmReady(false); return fallbackOnly(); } }; void boot(); return () => { cancelled = true; clearTimeout(timer); cancelAnimationFrame(raf); }; }, []);
  const stateAction: Action = state === "thinking" ? "think" : state === "idle" ? fallbackAction : state === "listening" ? "wave" : "idle";
  return <div ref={mountRef} className="jarvis-3d-layer">{!vrmReady && visibleFallback(stateAction, liveAppearance)}</div>;
}
